@using Microsoft.AspNetCore.Http
@{
    var path = $"{Context.Request.Path}";
    bool IsAdmin = path.Contains("/Admin/");
    

    if (IsAdmin)
    {
        Layout = "_AdminLayout";
    }
     

}
